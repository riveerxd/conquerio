services:
  backend:
    environment:
      ASPNETCORE_ENVIRONMENT: Development

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      BACKEND_ORIGIN: http://backend:8080
    depends_on:
      - backend
